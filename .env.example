# =============================================================================
# CLIENT-SIDE (Vite — stored in .env.local)
# =============================================================================

# Convex deployment URL (from `npx convex dev`)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Clerk (https://dashboard.clerk.com → API Keys)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Paddle client-side (https://vendors.paddle.com → Developer Tools → Authentication)
VITE_PADDLE_CLIENT_TOKEN=test_...
VITE_PADDLE_SANDBOX=true

# Paddle price IDs (https://vendors.paddle.com → Catalog → Prices)
VITE_PADDLE_SINGLE_PRICE_ID=pri_...
VITE_PADDLE_SUBSCRIPTION_PRICE_ID=pri_...

# RevenueCat entitlement ID to poll after Paddle checkout (defaults to "premium")
VITE_REVENUECAT_ENTITLEMENT_ID=premium

# =============================================================================
# SERVER-SIDE (Convex — set in the Convex dashboard, NOT in .env.local)
#
# Add these in your Convex dashboard:
#   https://dashboard.convex.dev → Settings → Environment Variables
#
# Or via the CLI:
#   npx convex env set VARIABLE_NAME value
# =============================================================================

# Clerk (https://dashboard.clerk.com → API Keys)
# CLERK_JWT_ISSUER_DOMAIN=https://your-clerk-instance.clerk.accounts.dev

# RevenueCat (https://app.revenuecat.com → Project → API Keys)
# REVENUECAT_API_KEY=sk_...
# REVENUECAT_WEBHOOK_AUTH_KEY=your-webhook-auth-key

# RevenueCat project ID — only needed if using virtual currency
# (https://app.revenuecat.com → Project → Settings)
# REVENUECAT_PROJECT_ID=proj...

# Paddle server-side (https://vendors.paddle.com → Developer Tools → Authentication)
# PADDLE_API_KEY=pdl_...
# PADDLE_SANDBOX=true
